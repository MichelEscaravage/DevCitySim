<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DevCitySim.BuildingCitizens"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevCitySim"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:localData="using:DevCitySim.Data.Classes"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="citizenName" Grid.Row="0" FontSize="20" FontWeight="Bold" Padding="10" />

        <ListView x:Name="buildingCitizensListView" Padding="10" Margin="0,10,0,10" Grid.Row="1" VerticalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="localData:BuildingCitizen">
                    <StackPanel Background="#e0e0e0" Padding="10" Margin="0,0,0,10" CornerRadius="8">
                        <TextBlock Text="{x:Bind Citizen.Name}" TextWrapping="Wrap" Margin="0,5,0,0" Foreground="Black"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
